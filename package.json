{
  "name": "calculatethis",
  "version": "1.0.0",
  "description": "Faceless calculator hub with hybrid frontend/backend",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
 "dependencies": {
  "express": "^4.18.2",
  "clsx": "^2.x.x",
  "tailwind-merge": "^2.x.x",
  "class-variance-authority": "^1.x.x",
  "tailwind-variants": "^1.x.x",
  "lucide-react": "^0.x.x"
}
